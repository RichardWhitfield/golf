# The Function URL from the `golf-store` stack (see infra/README.md). Public, not a secret —
# the browser has to call it, so it ships in `dist/` either way.
VITE_API_URL=https://xxxx.lambda-url.ap-southeast-2.on.aws
