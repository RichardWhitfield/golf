.playwright-mcp/
.superpowers/
node_modules/
dist/

# Local environment. `VITE_API_URL` is public by design, so this is habit rather than secrecy —
# the committed reference is `.env.example`.
.env
.env.local

# Infrastructure build output. Both are regenerated by one command; see infra/README.md.
infra/.aws-sam/
infra/packaged.yaml
